# Committed (not secret). The site is served from the custom domain
# vinamra.dev at the root, so no basePath is needed. If the custom domain
# is ever removed and the site falls back to the GitHub Pages project URL
# (https://<user>.github.io/<repo-name>/), set this back to "/<repo-name>".
NEXT_PUBLIC_BASE_PATH=
